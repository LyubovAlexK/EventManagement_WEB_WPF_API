<UserControl x:Class="EventManagmentApp.ZoomSettingPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EventManagmentApp"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0"
                   Text="Выберите размер элементов"
                   Style="{StaticResource H1Text}"
                   Foreground="#1E293B"
                   Margin="0,0,0,30"/>

        <!-- Выбор размера -->
        <Grid Grid.Row="1" Margin="0,0,0,40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="Размер"
                       Style="{StaticResource H2Text}"
                       Foreground="#1E293B"
                       VerticalAlignment="Center"/>

            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <!-- Маленький -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,30,0">
                    <TextBlock Text="Маленький"
                               Style="{StaticResource H3Text}"
                               Foreground="#1E293B"
                               VerticalAlignment="Center"
                               Margin="0,0,10,0"/>
                    <ToggleButton x:Name="LittleUIToggle"
                                  Style="{StaticResource SettingToggleButton}"
                                  Tag="Small"
                                  Checked="SizeToggle_Checked"
                                  Unchecked="SizeToggle_Checked"/>
                </StackPanel>

                <!-- Средний -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,30,0">
                    <TextBlock Text="Средний"
                               Style="{StaticResource H3Text}"
                               Foreground="#1E293B"
                               VerticalAlignment="Center"
                               Margin="0,0,10,0"/>
                    <ToggleButton x:Name="MiddleUIToggle"
                                  Style="{StaticResource SettingToggleButton}"
                                  Tag="Medium"
                                  Checked="SizeToggle_Checked"
                                  Unchecked="SizeToggle_Checked"
                                  IsChecked="True"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>